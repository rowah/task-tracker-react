import Header from "./components/Header";
function App() {
  return <Header />; //we can pass anything (props) and to retrieve it the function takes in props as param inside component
}

export default App;
